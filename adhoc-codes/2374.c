#include<stdio.h>

    int main(){

        int n, m, diferenca;

        scanf("%d", &n);
        scanf("%d", &m);

        diferenca = n - m;

        printf("%d\n", diferenca);

        return 0;
    }